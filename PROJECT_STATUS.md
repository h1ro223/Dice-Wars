# Dice Wars v1.4 - プロジェクト現状まとめ
**作成日: 2026-04-07**

---

## プロジェクト概要

崩壊：スターレイルのイベントミニゲームを参考に作ったサイコロカードバトルゲーム。
HTML/CSS/JS のみで動作。オンライン対戦はNode.js+WebSocketサーバー（Renderでホスト）。

- **GitHub:** https://github.com/h1ro223/Dice-Wars
- **プレイURL:** https://h1ro223.github.io/Dice-Wars/
- **オンラインサーバーURL:** https://dice-wars-0rcs.onrender.com

---

## ファイル構成

```
Dice Wars/
├── index.html       # メインHTML（全画面・モーダル含む）
├── script.js        # ゲームロジック全般（約1220行）
├── style.css        # スタイル（約1100行）
├── Manual.txt       # キャラ・ルール仕様書
├── README.md        # GitHubのREADME
├── .gitignore       # node_modules除外
└── server/
    ├── server.js    # WebSocketサーバー（Renderにデプロイ）
    └── package.json # 依存パッケージ（ws）
```

---

## 現在のバージョン: v1.4

### 実装済み機能
- VS CPU戦（キャラ選択 → コインフリップ → ターン制バトル）
- ローカルマルチプレイ（1台の端末で2人）
- オンラインプレイ（WebSocket、Render運用）
- 9体のキャラクター（それぞれ固有能力あり）
- オーディオシステム（BGM・SE、Web Audio API）
- BGM/SE設定トグル
- 更新履歴モーダル

### v1.4での主な変更
- バランス調整（ホースラ・ジャスパー・クライシス）
- 新キャラ3体追加（ドラン・ライアン・オーガスト）
- 引き分け条件追加（同時に両者HP0）
- ライアンのDEF+1バフはHP5以下の時のみ（HP6以上で取り消し）

---

## オンラインマルチプレイの実装状況

### アーキテクチャ（最新版）
**「ロビーマッチング方式」** に全面刷新済み（ルーム制は廃止）

#### 接続フロー
1. URLをデフォルト値 `wss://dice-wars-0rcs.onrender.com` で接続
2. プレイヤー名（英数字6文字以内）を入力して「登録」
3. ロビー画面でオンライン中のプレイヤー一覧が表示される
4. 対戦したいプレイヤーをタップ→相手も自分を選ぶとマッチ成立
5. デッキ選択 → バトル開始（ホストのみ開始ボタンを押せる）

#### サーバー（server/server.js）の概要
- プレイヤー名をキーに `Map` で管理
- 接続時: `set_name` → 名前登録 → `player_list` をロビー全員にブロードキャスト
- 選択時: `select_opponent` → 相互選択で `match_created` を両者に送信
- ゲーム中: `game_action` をラップして相手に転送するリレー方式
- 切断時: マッチ相手に `opponent_disconnected` を通知

#### クライアント同期の仕組み（script.js）
- `isMyTurn()` でホスト/ゲストのターンを判定
- ダイスロール時: 自分側のみ乱数生成 → `dice_values` で相手に送信
- ダイス選択時: `dice_toggle` を送信して相手画面も更新
- リロール時: `reroll` でリロール後の全ダイス値を送信
- OKボタン時: `confirm` でフェーズ進行を同期
- コインフリップ: ホストが結果生成 → ゲストは反転して使用（不整合ゼロ）

---

## 解決済みバグ（今回のセッションで修正）

| # | バグ内容 | 解決方法 |
|---|---------|---------|
| 1 | ゲスト側でバトル開始ボタンが押せた | `checkOnlineReady()` でゲストは常にdisabled |
| 2 | リロール後のダイスが相手側で更新されない | `dice_values` / `reroll` で全ダイス値を送信 |
| 3 | ゲスト防御時にサイコロ選択UIが動かない | `isMyTurn()` 判定 + ボタン統一 |
| 4 | コインフリップの先攻/後攻が同期されない | ホストのみ生成→ゲストは反転use |
| 5 | 切断後に接続ボタンがグレーアウトしたまま | モーダル再表示時にUI全リセット |

## 新機能（今回のセッションで追加）

| # | 機能 |
|---|------|
| 1 | ボタン統一（左パネル廃止、全モードで右パネルのみ使用） |
| 2 | ロビーマッチング（ルームコード制を廃止） |
| 3 | デフォルトサーバーURLをコードに埋め込み |
| 4 | タイトル画面に⚙設定ボタン追加 |
| 5 | 設定モーダルに「🔄ページを更新する」ボタン追加 |
| 6 | CPU AIのリロール改善（低い出目を視覚的にリロール） |

---

## 現在残っている問題・未確認事項

### ⚠️ 重要: Renderのサーバーがまだ古い
**GitHubにpushができていれば新しいserver.jsがデプロイされているはず。**
まだpushしていない場合は以下が必要：
```bash
cd "Dice Wars"
git add .
git commit -m "v1.4: ロビーマッチング実装、バグ修正"
git push
```
Renderは自動デプロイなので、push後数分でサーバーが更新される。

### 🔍 未テスト（実機2台での確認が必要）
- ロビー → マッチング → デッキ選択 → バトルの一連のフロー
- リロール・OK・コインフリップの同期
- バトル中の切断→復帰

### 📝 README.mdの「不具合のお知らせ」が古い
v1.4修正前の不具合リストが書いてある。修正済みなので書き換えが必要。

---

## キャラクター一覧（v1.4）

| # | 名前 | HP | ATK | DEF | 特徴 |
|---|------|-----|-----|-----|------|
| 1 | ガルム | 22 | 3 | 2 | ATK中に同じ目3つ → ダメージ+3、1増えるたびに+3 |
| 2 | ホースラ | 25 | 3 | 2 | 防御時リロール1回。ダメージ0→HP5回復 |
| 3 | ジャスパー | 20 | 3 | 2 | ATK中に同じ目2つ → +5、1増えるたびに+3 |
| 4 | クライシス | 20 | 3 | 2 | 自身HPが最大でない時"イカサマ"付与（選択ダイス数+1） |
| 5 | ドラン | 20 | 3 | 3 | 偶数の目が出るたびダメージ+2（防御側も同様） |
| 6 | ライアン | 24 | 4 | 2 | 防御後ダメージ0→HP5回復。HP5以下でDEF+1（HP6以上で解除） |
| 7 | オーガスト | 20 | 4 | 2 | 自身の出目最大値以上の数字が1つでもあればダメージ+5 |
| 8 | ポルテコ | 18 | 4 | 3 | 防御フェーズで選択ダイスの最大値が奇数→ダメージ半減 |
| 9 | ヴァルカン | 14 | 5 | 2 | 攻撃後自分もダメージ（攻撃ダメージの1/5切り捨て） |

---

## 開発環境・ツール

- エディタ: Visual Studio Code または Google Antigravity
- サーバー: Node.js + ws ライブラリ（Renderで無料ホスト）
- フロントエンド: HTML/CSS/JS（フレームワークなし）
- バージョン管理: GitHub（h1ro223/Dice-Wars）
- AIアシスタント: Google Antigravity（Gemini / Claude / GPT-OSS使用）

---

## 別PCで作業を再開するには

1. GitHubからクローン or ZIPダウンロード
2. `Dice Wars/server/` フォルダで `npm install`
3. ローカルテスト: `node server.js` → `ws://localhost:8080` で接続
4. VS Codeの「Live Server」拡張でindex.htmlを開く

**AIに引き継ぐ場合の伝え方:**
> 「Dice Wars v1.4のオンラインマルチプレイ開発の続きです。このまとめファイルを読んで状況を把握してください。今やりたいことは〇〇です。」
